<%= form_for(@rapper_song) do |f| %>
  <% if @rapper_song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rapper_song.errors.count, "error") %> prohibited this rapper_song from being saved:</h2>

      <ul>
      <% @rapper_song.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rapper %><br>
    <%= f.text_field :rapper %>
  </div>
  <div class="field">
    <%= f.label :song %><br>
    <%= f.text_field :song %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
